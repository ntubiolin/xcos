{
    "data_loader": {
        "type": "MnistDataLoader",
        "args": {
            "data_dir": "../datasets/mnist",
            "batch_size": 32,
            "validation_split": 0,
            "num_workers": 4,
            "name": "mnist"
        }
    },
    "valid_data_loaders": {
        "0": {
            "type": "MnistDataLoader",
            "args": {
                "data_dir": "../datasets/mnist",
                "batch_size": 32,
                "num_workers": 4,
                "shuffle": false,
                "name": "all_mnist"
            }
        }
    }
}
